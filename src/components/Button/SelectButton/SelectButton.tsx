import { SelectButtonProps } from '@/types/selectButton.type';

function SelectButton(props: SelectButtonProps) {
  const { selected, btnHeight } = props;

  return <button>HI</button>;
}

export default SelectButton;
