import styled from 'styled-components';

export const MainTitle = styled.div`
  #title-text {
color: var(--black-title);
font-family: Figtree;
font-size: 2.5rem;
font-style: normal;
font-weight: 700;
line-height: 3.5rem;
  }

  #desc-text {
color:var(--black-400);
font-size: 1.125rem;
font-style: normal;
font-weight: 400;
line-height: 1.5rem;
  }
`;