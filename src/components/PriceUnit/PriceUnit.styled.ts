import styled from 'styled-components';

export const PriceUnitContainer = styled.div`
  width: max-content;
  height: 1.75rem;

  #price {
    color: var(--blue-main);
    font-size: 1.25rem;
    font-weight: 600;
    line-height: 1.75rem;
  }

  #unit {
    font-weight: 400;
    line-height: 1.375rem;
  }
`;
